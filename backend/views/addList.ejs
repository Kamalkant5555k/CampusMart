<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Add Listing | CampusMart</title>
  <style>
    body { font-family: Arial, sans-serif; background:#f4f6f8; }
    .container { width: 420px; margin: 40px auto; background:#fff; padding:20px; border-radius:8px; }
    h2 { text-align:center; }
    input, textarea, select, button {
      width: 100%;
      margin-top: 10px;
      padding: 8px;
    }
    button {
      background: #2563eb;
      color: white;
      border: none;
      cursor: pointer;
    }
    button:hover { background:#1d4ed8; }
  </style>
</head>
<body>

<div class="container">
  <h2>Add New Listing</h2>

  <!-- Listing Form -->
<form action="/addNewList" method="POST" enctype="multipart/form-data">



    <!-- Title -->
    <input 
      type="text" 
      name="title" 
      placeholder="Item title" 
      required />

    <!-- Description -->
    <textarea 
      name="description" 
      placeholder="Item description" 
      rows="4"
      required></textarea>

    <!-- Price -->
    <input 
      type="number" 
      name="price" 
      placeholder="Price (₹)" 
      required />

    <!-- Category -->
    <select name="category" required>
      <option value="">Select Category</option>
      <option value="Books">Books</option>
      <option value="Electronics">Electronics</option>
      <option value="Hostel Items">Hostel Items</option>
      <option value="Other items">Other items</option>
    </select>

    <!-- Condition -->
    <select name="condition">
      <option value="Used">Used</option>
      <option value="Like New">Like New</option>
      <option value="New">New</option>
    </select>

    <!-- Images (comma separated URLs) -->
    <div mb-3>
  <label>Upload Image</label>
 <input type="file" name="listing[image]" required>

</div>


    <!-- Submit -->
    <button type="submit">Add Listing</button>

  </form>
</div>

</body>
</html>
