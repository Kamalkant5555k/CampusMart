<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listing Details</title>
</head>
<body>
    <div class="main">
        
        <h2><%= listing.title %></h2>
        <img src="<%= listing.img %>" alt="<%= listing.title %>" style="max-width: 300px;">
       
        <p><strong>Description:</strong> <%= listing.description %></p>
        <p><strong>Price:</strong> ₹ <%= listing.price %></p>
        <p><strong>Category:</strong> <%= listing.category %></p>
        <p><strong>Condition:</strong> <%= listing.condition %></p>
        <div class="all-btn">
        <a href="/listings">Back to Listings</a>
        <form method="POST" action="/listings/<%= listing._id %>?_method=DELETE" onsubmit="return confirm('Are you sure you want to delete this listing?');">
      <button type="submit">
         Delete
      </button>
    </form>
        <a  href="/listings/<%= listing._id %>/edit">Edit Listing</a>
        </div>
    </div>
    
</body>
</html>